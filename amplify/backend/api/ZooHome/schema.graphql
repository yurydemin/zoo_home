type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: String!
  shelterID: String!
}

type Shelter @model @auth(rules: [{allow: public}]) {
  id: ID!
  location: String
  title: String
  description: String
  contact: String!
  avatarKey: String
  imageKeys: [String]!
  userID: String!
  pets: [Pet!] @connection(keyName: "byShelter", fields: ["id"])
}

type Pet @model @auth(rules: [{allow: public}]) @key(name: "byShelter", fields: ["shelterID"]) {
  id: ID!
  status: PetStatus
  kind: PetKind
  title: String
  description: String
  imageKeys: [String]!
  date: AWSDateTime!
  shelterID: ID!
}

enum PetKind {
  CAT
  DOG
  OTHER
}

enum PetStatus {
  OPENED
  CLOSED
  RESERVED
}
 